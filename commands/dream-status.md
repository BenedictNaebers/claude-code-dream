---
description: Show status of the nightly dream agent (last run, in-progress state, pending review items).
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/dream_status.sh:*)"]
---

# Dream Status

Run `${CLAUDE_PLUGIN_ROOT}/scripts/dream_status.sh` and show its output verbatim. Do not interpret, reformat, or add commentary — the script is the source of truth.
